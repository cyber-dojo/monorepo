
version: 1
trail:
  attestations:
    - { name: pull-request, type: pull_request }
  artifacts:
    - name: dashboard
      attestations:
        - { name: rubocop,             type: junit }
        - { name: snyk-container-scan, type: generic }
