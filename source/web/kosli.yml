
version: 1
trail:
  attestations:
    - { name: pull-request, type: pull_request }
  artifacts:
    - name: web
      attestations:
        - { name: lint,      type: generic }
        - { name: unit-test, type: junit }
